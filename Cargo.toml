[package]
name = "golf_sim"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
rand = "0.9.2"
rayon = "1.12.0"

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
