[package]
name = "golf_sim"
version = "0.1.0"
edition = "2021"
default-run = "golf_sim"

[dependencies]
candle-core = "0.11.0"
candle-nn = "0.11.0"
clap = { version = "4.5", features = ["derive"] }
rand = "0.9.2"
rayon = "1.12.0"

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
